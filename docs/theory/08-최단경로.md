# 최단경로

## BFS vs 다익스트라
- 간선 가중치가 모두 같음: BFS
- 가중치가 다르고 음수 없음: 다익스트라

## 다익스트라 핵심
- 우선순위 큐로 현재 가장 짧은 거리 노드부터 확정
- 거리 배열 `dist` 유지

## 기본 패턴
1. `dist`를 무한대로 초기화
2. 시작점만 0
3. 힙에서 꺼내며 인접 노드 완화(relax)

## 실수 포인트
- 이미 더 짧은 값이 있는 노드 재처리
- 인접 리스트 구조 실수 (`(cost, node)` 순서 혼동)
