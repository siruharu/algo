# 정렬과 이분탐색

## 정렬
- `sorted(arr)` 또는 `arr.sort()` 사용
- key를 이용해 다중 조건 정렬 가능

## 이분탐색을 쓰는 조건
- 데이터가 정렬되어 있음
- "가능/불가능"이 경계로 나뉘는 단조성 존재

## 패턴
1. 탐색 구간 `lo, hi` 설정
2. 중간값 `mid` 계산
3. 조건 만족 여부에 따라 범위 축소

## 실수 포인트
- `while lo <= hi`와 `while lo < hi` 혼용
- mid 계산 후 갱신 규칙 누락으로 무한 루프
