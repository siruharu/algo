# DP 기초

## 언제 쓰나
- 큰 문제를 작은 문제로 나눌 수 있음
- 같은 계산이 반복됨(중복 부분 문제)

## 설계 순서
1. 상태 정의: `dp[i]`가 무엇인지 한 문장으로
2. 점화식 정의
3. 초기값(base case) 설정
4. 계산 순서(작은 값 -> 큰 값)

## 예시 형태
- 1차원: `dp[i] = max(dp[i-1], dp[i-2] + arr[i])`
- 2차원: 격자/문자열 문제

## 실수 포인트
- 상태 정의가 모호함
- 초기값 누락
